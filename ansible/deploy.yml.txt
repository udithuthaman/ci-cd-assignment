- hosts: localhost
  tasks:
    - name: Stop old container (if running)
      docker_container:
        name: employee-service
        state: absent

    - name: Run new container
      docker_container:
        name: employee-service
        image: employee-service
        state: started
        published_ports:
          - "8095:8089"
